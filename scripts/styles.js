/*
 * The module vars
 */

// Global module id
const MODULE_ID = 'custom-styles'

// HTML/Handlebar templates
const TEMPLATES = Object.freeze({
    FORM: `modules/${MODULE_ID}/templates/form.hbs`
})

export { MODULE_ID, TEMPLATES }
